# Build artifacts
build/
*.o
*.obj
*.so
*.dylib
*.dll
*.exe

# CUDA / compiler intermediates
*.cubin
*.ptx
*.fatbin
*.i
*.ii

# LaTeX build artifacts
*.aux
*.log
*.out
*.synctex.gz
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.nav
*.snm
*.vrb

# Presentation script — personal prep document, not a deliverable.
slides/script.html
slides/script.md

# Proposal is a frozen submitted artifact — only the PDF is tracked.
# The .tex is kept locally for reference but not versioned.
proposal/*.tex

# CLAUDE.md is local-only project notes, not part of the repo.
CLAUDE.md
docs/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Nsight / profiling outputs
*.nsight-cuprof-report
*.ncu-rep
*.nsys-rep
*.qdrep

# Raw benchmark capture — reproducible from `build/bench --csv`, not a
# submission artifact. Derived figures under report/figs/ are tracked.
.bench-raw/

# 10k-MNIST bulk parity fixtures (~32 MB). Reproducible from
# `python3 tools/dump_fixtures.py --bulk --count 10000`.
fixtures/mnist10k_*.bin
